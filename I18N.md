# 国际化模块说明与使用文档

本文档说明 [packages/platform/core/locales/src/i18n.ts](packages/platform/core/locales/src/i18n.ts) 的实现逻辑、目录约定和业务接入方式。

该模块基于：

- Vue 3
- vue-i18n
- Vite `import.meta.glob`

用于统一管理平台核心包和业务应用的多语言资源。

---

## 1. 文件职责

[packages/platform/core/locales/src/i18n.ts](packages/platform/core/locales/src/i18n.ts) 主要负责：

1. 创建全局 `vue-i18n` 实例。
2. 自动扫描核心包内的本地语言包。
3. 初始化 Vue 应用的国际化能力。
4. 支持业务应用通过 `loadMessages` 扩展自己的语言包。
5. 切换当前语言，并同步更新 `<html lang="...">`。

---

## 2. 核心源码说明

### 2.1 依赖导入

```ts
import type { App } from 'vue';

import { unref } from 'vue';
import { createI18n, Locale } from 'vue-i18n';
import { loadLocalesMapFromDir } from './tools';
import { LoadMessageFn, LocaleSetupOptions, SystemLanguage } from './typing';
```

说明：

- `App`：Vue 应用实例类型，用于 `setupI18n(app)`。
- `unref`：用于读取可能是 `ref` 的值。
- `createI18n`：创建 vue-i18n 实例。
- `Locale`：vue-i18n 的语言类型，例如 `zh-CN`、`en-US`。
- `loadLocalesMapFromDir`：项目封装的语言包加载工具，定义在 [packages/platform/core/locales/src/tools.ts](packages/platform/core/locales/src/tools.ts)。
- `LoadMessageFn`：业务扩展语言包加载函数类型。
- `LocaleSetupOptions`：初始化配置项类型。
- `SystemLanguage`：系统语言枚举，定义在 [packages/platform/core/locales/src/typing.ts](packages/platform/core/locales/src/typing.ts)。

---

### 2.2 创建全局 i18n 实例

```ts
const i18n = createI18n({
  globalInjection: true,
  legacy: false,
  locale: '',
  messages: {},
});
```

配置说明：

#### `globalInjection: true`

开启全局注入后，Vue 模板中可以直接使用 `$t`、`$te` 等方法。

示例：

```vue
<template>
  <h1>{{ $t('login.welcomeBack') }}</h1>
</template>
```

当前项目中已有使用示例：

[packages/platforms/engine/src/views/login/login.vue](packages/platforms/engine/src/views/login/login.vue)

#### `legacy: false`

关闭 legacy 模式，启用 Vue 3 Composition API 模式。

因此设置语言时需要写成：

```ts
i18n.global.locale.value = locale;
```

而不是：

```ts
i18n.global.locale = locale;
```

#### `locale: ''`

初始化时不直接指定语言。

真正的默认语言会在 `setupI18n` 中加载：

```ts
await loadLocaleMessages(defaultLocale);
```

#### `messages: {}`

初始语言包为空。

语言包通过 Vite 的 `import.meta.glob` 动态扫描和加载。

---

### 2.3 扫描本地语言包

```ts
const modules = import.meta.glob('./langs/**/*.json');
```

该语句会扫描当前文件所在目录下的语言包：

```txt
src/langs/**/*.json
```

推荐目录结构：

```txt
src/
  langs/
    zh-CN/
      index.json
      common.json
    en-US/
      index.json
      common.json
```

`import.meta.glob` 返回的是一个模块映射对象，结构大致如下：

```ts
{
  './langs/zh-CN/index.json': () => import('./langs/zh-CN/index.json'),
  './langs/zh-CN/common.json': () => import('./langs/zh-CN/common.json'),
  './langs/en-US/index.json': () => import('./langs/en-US/index.json')
}
```

每个值都是一个异步导入函数。

---

### 2.4 生成语言包加载映射

```ts
const localesMap = loadLocalesMapFromDir(/\.\/langs\/([^/]+)\/(.*)\.json$/, modules);
```

这行代码将 `import.meta.glob` 的扫描结果转换成按语言分组的加载函数。

正则说明：

```ts
/\.\/langs\/([^/]+)\/(.*)\.json$/
```

对于路径：

```txt
./langs/zh-CN/index.json
```

匹配结果为：

- `locale = 'zh-CN'`
- `fileName = 'index'`

对于路径：

```txt
./langs/en-US/common.json
```

匹配结果为：

- `locale = 'en-US'`
- `fileName = 'common'`

最终 `localesMap` 结构类似：

```ts
{
  'zh-CN': async () => ({
    default: {
      // zh-CN 目录下所有 json 文件合并后的内容
    }
  }),
  'en-US': async () => ({
    default: {
      // en-US 目录下所有 json 文件合并后的内容
    }
  })
}
```

注意：当前 [packages/platform/core/locales/src/tools.ts](packages/platform/core/locales/src/tools.ts) 的实现会把同一语言目录下的多个 JSON 文件内容直接合并到同一个对象中，而不是按文件名嵌套。

例如：

```json
// common.json
{
  "common": {
    "confirm": "确认"
  }
}
```

```json
// login.json
{
  "login": {
    "welcomeBack": "欢迎回来"
  }
}
```

最终合并结果为：

```ts
{
  common: {
    confirm: '确认'
  },
  login: {
    welcomeBack: '欢迎回来'
  }
}
```

---

### 2.5 业务扩展语言包加载函数

```ts
let loadMessages: LoadMessageFn;
```

`loadMessages` 用于保存业务应用传入的语言包加载函数。

核心包可以提供基础语言包，业务应用可以提供自己的语言包，然后在初始化时合并。

类型定义在 [packages/platform/core/locales/src/typing.ts](packages/platform/core/locales/src/typing.ts)：

```ts
export type LoadMessageFn = (lang: SystemLanguage) => Promise<Record<string, string> | undefined>;
```

当前项目的业务使用示例在：

[packages/platforms/engine/src/i18n/i18n.ts](packages/platforms/engine/src/i18n/i18n.ts)

```ts
async function loadMessages(lang: SystemLanguage) {
  const [appLocaleMessages] = await Promise.all([localesMap[lang]?.()]);
  return appLocaleMessages?.default;
}
```

---

### 2.6 设置当前语言

```ts
function setI18nLanguage(locale: Locale) {
  i18n.global.locale.value = locale;

  document?.querySelector('html')?.setAttribute('lang', locale);
}
```

该函数做两件事：

1. 设置 vue-i18n 当前语言。
2. 设置 HTML 根节点的 `lang` 属性。

例如切换到中文后，页面会变成：

```html
<html lang="zh-CN">
```

这样做的好处：

- 有利于浏览器识别页面语言。
- 有利于无障碍工具，例如屏幕阅读器。
- 有利于 SEO。
- 有利于浏览器自动翻译判断。

---

### 2.7 加载语言包

```ts
const loadLocaleMessages = async (lang: SystemLanguage) => {
  // 语言包已存在
  if (unref(i18n.global.locale) === lang) {
    return setI18nLanguage(lang);
  }

  // 语言包不存在
  // 加载语言包
  const message = await localesMap[lang]?.();
  if (message?.default) {
    i18n.global.setLocaleMessage(lang, message.default);
  }

  // 加载合并语言包
  const mergeMessage = await loadMessages(lang);
  i18n.global.mergeLocaleMessage(lang, mergeMessage);

  // 设置语言
  return setI18nLanguage(lang);
};
```

执行流程：

1. 判断当前语言是否已经是目标语言。
2. 如果已经是目标语言，则不重复加载，直接设置 `<html lang>` 后返回。
3. 加载核心包内置语言包。
4. 调用业务应用传入的 `loadMessages`。
5. 将业务语言包合并进当前语言。
6. 设置当前语言。

注意：

```ts
if (unref(i18n.global.locale) === lang)
```

这里判断的是“当前语言是否等于目标语言”，并不是严格意义上的“语言包是否已经加载过”。

如果后续需要支持运行时多次切换语言，并避免重复加载，可以维护一个已加载语言集合。

---

### 2.8 初始化国际化

```ts
const setupI18n = async (app: App, options: LocaleSetupOptions = {}) => {
  const { defaultLocale = SystemLanguage.ZhCn } = options;

  // app可以自行扩展一些第三方库和组件库的国际化
  loadMessages = options.loadMessages || (async () => ({}));

  app.use(i18n);

  await loadLocaleMessages(defaultLocale);
};
```

`setupI18n` 是对外暴露的初始化入口。

它会：

1. 读取默认语言，未传入时默认使用 `SystemLanguage.ZhCn`。
2. 保存业务应用传入的 `loadMessages`。
3. 安装 vue-i18n 插件。
4. 加载默认语言包并切换语言。

初始化时建议使用 `await`：

```ts
await setupI18n(app);
```

原因是语言包是异步加载的。如果不等待完成，页面首次渲染时可能短暂显示翻译 key。

---

### 2.9 导出

```ts
export { i18n, setupI18n };
```

当前只导出：

- `i18n`：全局国际化实例。
- `setupI18n`：国际化初始化函数。

如果后续需要在页面中动态切换语言，可以考虑导出 `loadLocaleMessages`。

---

## 3. 完整注释版代码

```ts
import type { App } from 'vue';

import { unref } from 'vue';
import { createI18n, Locale } from 'vue-i18n';
import { loadLocalesMapFromDir } from './tools';
import { LoadMessageFn, LocaleSetupOptions, SystemLanguage } from './typing';

/**
 * 创建全局 i18n 实例。
 *
 * globalInjection: true
 * - 开启全局注入，模板中可以直接使用 $t、$te。
 *
 * legacy: false
 * - 使用 Vue I18n Composition API 模式。
 * - i18n.global.locale 是 ref，设置语言时需要使用 .value。
 *
 * locale: ''
 * - 初始化时暂不指定语言。
 * - 实际语言会在 setupI18n 中加载并设置。
 *
 * messages: {}
 * - 初始语言包为空，通过 import.meta.glob 动态加载。
 */
const i18n = createI18n({
  globalInjection: true,
  legacy: false,
  locale: '',
  messages: {},
});

/**
 * 使用 Vite import.meta.glob 扫描当前包内的语言包文件。
 *
 * 约定目录结构：
 *
 * src/langs/zh-CN/index.json
 * src/langs/en-US/index.json
 */
const modules = import.meta.glob('./langs/**/*.json');

/**
 * 本地语言包映射。
 *
 * 将 import.meta.glob 的扫描结果转换成按语言分组的加载函数：
 *
 * {
 *   'zh-CN': async () => ({ default: { ... } }),
 *   'en-US': async () => ({ default: { ... } })
 * }
 */
const localesMap = loadLocalesMapFromDir(/\.\/langs\/([^/]+)\/(.*)\.json$/, modules);

/**
 * 外部语言包加载函数。
 *
 * 由 setupI18n 的 options.loadMessages 传入，
 * 用于让业务应用加载并合并自己的语言包。
 */
let loadMessages: LoadMessageFn;

/**
 * 设置当前语言。
 *
 * @param locale 要切换的语言，例如 zh-CN、en-US
 */
function setI18nLanguage(locale: Locale) {
  // legacy: false 时，locale 是 ref，需要通过 .value 修改
  i18n.global.locale.value = locale;

  // 同步更新 html lang 属性，便于 SEO、无障碍和浏览器识别语言
  document?.querySelector('html')?.setAttribute('lang', locale);
}

/**
 * 加载并切换语言。
 *
 * 执行流程：
 * 1. 如果当前语言已经是目标语言，则直接返回
 * 2. 加载核心包内置语言包
 * 3. 加载业务应用扩展语言包
 * 4. 合并业务语言包
 * 5. 设置当前语言
 */
const loadLocaleMessages = async (lang: SystemLanguage) => {
  // 当前语言已是目标语言，无需重复加载
  if (unref(i18n.global.locale) === lang) {
    return setI18nLanguage(lang);
  }

  // 加载核心包内置语言包
  const message = await localesMap[lang]?.();
  if (message?.default) {
    i18n.global.setLocaleMessage(lang, message.default);
  }

  // 加载业务应用扩展语言包
  const mergeMessage = await loadMessages(lang);
  i18n.global.mergeLocaleMessage(lang, mergeMessage);

  // 语言包加载完成后再切换语言
  return setI18nLanguage(lang);
};

/**
 * 初始化国际化。
 *
 * @param app Vue 应用实例
 * @param options 国际化初始化配置
 */
const setupI18n = async (app: App, options: LocaleSetupOptions = {}) => {
  // 默认语言，未传入时使用简体中文
  const { defaultLocale = SystemLanguage.ZhCn } = options;

  // 保存业务扩展语言包加载函数；未传入时使用空函数
  loadMessages = options.loadMessages || (async () => ({}));

  // 安装 vue-i18n 插件
  app.use(i18n);

  // 加载默认语言包并设置当前语言
  await loadLocaleMessages(defaultLocale);
};

export { i18n, setupI18n };
```

---

## 4. 使用文档

### 4.1 语言包目录规范

推荐按照语言分目录：

```txt
src/i18n/langs/
  zh-CN/
    index.json
    common.json
  en-US/
    index.json
    common.json
```

核心包对应路径：

```txt
packages/platform/core/locales/src/langs/
```

业务应用对应路径示例：

```txt
packages/platforms/engine/src/i18n/langs/
```

---

### 4.2 语言包 JSON 格式

示例：

```json
{
  "login": {
    "welcomeBack": "欢迎回来",
    "subtitle": "立即登录以管理您的游戏项目和教程",
    "usernameLabel": "用户名/手机号",
    "usernamePlaceholder": "请输入您的账户",
    "passwordLabel": "密码",
    "passwordPlaceholder": "请输入密码",
    "loginButton": "立即登录",
    "thirdPartyLogin": "第三方快捷登录"
  }
}
```

模板中使用：

```vue
<template>
  <h1>{{ $t('login.welcomeBack') }}</h1>
  <p>{{ $t('login.subtitle') }}</p>
</template>
```

---

### 4.3 在业务应用中初始化

业务应用推荐封装自己的 `setupI18n`，用于加载业务侧语言包。

示例：

```ts
import type { LocaleSetupOptions } from '@locales';
import type { App } from 'vue';

import { setupI18n as coreSetup, loadLocalesMapFromDir, SystemLanguage } from '@locales';

const modules = import.meta.glob('./langs/**/*.json');
const localesMap = loadLocalesMapFromDir(/\.\/langs\/([^/]+)\/(.*)\.json$/, modules);

/**
 * 加载应用特有的语言包。
 * 后续也可以改造为从服务端加载翻译数据。
 */
async function loadMessages(lang: SystemLanguage) {
  const [appLocaleMessages] = await Promise.all([localesMap[lang]?.()]);
  return appLocaleMessages?.default;
}

export const setupI18n = async (app: App, options: LocaleSetupOptions = {}) => {
  await coreSetup(app, {
    defaultLocale: SystemLanguage.ZhCn,
    loadMessages,
  });
};
```

当前项目示例：

[packages/platforms/engine/src/i18n/i18n.ts](packages/platforms/engine/src/i18n/i18n.ts)

---

### 4.4 在应用入口中调用

推荐启动流程：

```ts
import { createApp } from 'vue';
import App from './App.vue';
import { setupI18n } from './i18n';

async function bootstrap() {
  const app = createApp(App);

  await setupI18n(app);

  app.mount('#app');
}

bootstrap();
```

当前项目是在应用初始化阶段调用：

[packages/platforms/engine/src/utils/app-init/index.ts](packages/platforms/engine/src/utils/app-init/index.ts)

```ts
// 国际化 i18n 配置
await setupI18n(app);
```

---

### 4.5 在 Vue 模板中使用

因为 `globalInjection` 设置为 `true`，所以模板中可以直接使用 `$t`。

```vue
<template>
  <div>
    <h1>{{ $t('login.welcomeBack') }}</h1>
    <p>{{ $t('login.subtitle') }}</p>
    <button>{{ $t('login.loginButton') }}</button>
  </div>
</template>
```

---

### 4.6 在 `<script setup>` 中使用

推荐使用 `useI18n`：

```vue
<script setup lang="ts">
import { useI18n } from 'vue-i18n';

const { t, te, locale } = useI18n();

const title = t('login.welcomeBack');
</script>
```

说明：

- `t`：翻译函数。
- `te`：判断某个 key 是否存在。
- `locale`：当前语言。

---

### 4.7 在普通 TypeScript 文件中使用

可以直接使用导出的 `i18n` 实例：

```ts
import { i18n } from '@locales';

const title = i18n.global.t('login.welcomeBack');
```

如果入口文件导出了 `$t`，也可以使用：

```ts
import { $t } from '@locales';

const title = $t('login.welcomeBack');
```

---

## 5. 新增语言

假设需要新增日语 `ja-JP`。

### 5.1 扩展语言枚举

修改 [packages/platform/core/locales/src/typing.ts](packages/platform/core/locales/src/typing.ts)：

```ts
export enum SystemLanguage {
  EnUs = 'en-US',
  ZhCn = 'zh-CN',
  JaJp = 'ja-JP',
}
```

### 5.2 新增语言包目录

核心语言包：

```txt
packages/platform/core/locales/src/langs/ja-JP/index.json
```

业务语言包：

```txt
packages/platforms/engine/src/i18n/langs/ja-JP/index.json
```

### 5.3 编写语言包

```json
{
  "login": {
    "welcomeBack": "お帰りなさい",
    "subtitle": "ログインしてプロジェクトを管理しましょう"
  }
}
```

### 5.4 指定默认语言

```ts
await setupI18n(app, {
  defaultLocale: SystemLanguage.JaJp,
});
```

如果业务包装层固定写死了默认语言，例如：

```ts
defaultLocale: SystemLanguage.ZhCn,
```

则需要改成支持透传：

```ts
defaultLocale: options.defaultLocale || SystemLanguage.ZhCn,
```

---

## 6. 扩展业务语言包

业务层可以通过 `loadMessages` 注入自己的语言包。

```ts
async function loadMessages(lang: SystemLanguage) {
  const appLocaleMessages = await localesMap[lang]?.();

  return {
    ...appLocaleMessages?.default,
  };
}
```

如果需要同时合并第三方组件库语言包，也可以在这里处理：

```ts
import zhCn from 'element-plus/es/locale/lang/zh-cn';
import en from 'element-plus/es/locale/lang/en';

async function loadMessages(lang: SystemLanguage) {
  const appLocaleMessages = await localesMap[lang]?.();

  const elementPlusMessages = {
    [SystemLanguage.ZhCn]: zhCn,
    [SystemLanguage.EnUs]: en,
  };

  return {
    ...appLocaleMessages?.default,
    elementPlus: elementPlusMessages[lang],
  };
}
```

注意：Element Plus 自身的国际化通常还需要配合 `ElConfigProvider` 的 `locale` 属性使用。是否需要合并到 vue-i18n，取决于项目对组件库国际化的封装方式。

---

## 7. 初始化流程

```txt
业务入口 initApp(app)
        |
        v
业务 setupI18n(app)
        |
        v
核心 setupI18n(app, options)
        |
        |-- 读取 defaultLocale，默认 zh-CN
        |-- 保存 options.loadMessages
        |-- app.use(i18n)
        |
        v
loadLocaleMessages(defaultLocale)
        |
        |-- 当前语言是否已经是目标语言？
        |       |
        |       |-- 是：setI18nLanguage(lang)
        |       |
        |       |-- 否：
        |             |
        |             |-- 加载核心 localesMap[lang]()
        |             |-- setLocaleMessage(lang, coreMessages)
        |             |-- 加载业务 loadMessages(lang)
        |             |-- mergeLocaleMessage(lang, businessMessages)
        |             |-- setI18nLanguage(lang)
        |
        v
完成初始化
```

---

## 8. 当前实现的注意点与建议

### 8.1 `loadLocaleMessages` 当前没有导出

当前只导出了：

```ts
export { i18n, setupI18n };
```

如果后续需要在页面里动态切换语言，建议导出：

```ts
export { i18n, setupI18n, loadLocaleMessages };
```

并在入口文件继续透出。

---

### 8.2 当前语言判断不等于语言包缓存判断

当前代码：

```ts
if (unref(i18n.global.locale) === lang) {
  return setI18nLanguage(lang);
}
```

实际判断的是“当前语言已经是目标语言”，不是“目标语言包已经加载”。

如果需要避免重复加载已经加载过的语言，可以维护缓存：

```ts
const loadedLanguages = new Set<Locale>();

if (loadedLanguages.has(lang)) {
  return setI18nLanguage(lang);
}

// 加载完成后
loadedLanguages.add(lang);
```

---

### 8.3 `mergeLocaleMessage` 建议兼容空值

当前 `LoadMessageFn` 允许返回 `undefined`：

```ts
Promise<Record<string, string> | undefined>
```

更稳妥的写法：

```ts
const mergeMessage = await loadMessages(lang);
if (mergeMessage) {
  i18n.global.mergeLocaleMessage(lang, mergeMessage);
}
```

---

### 8.4 语言包类型建议支持嵌套对象

当前类型是：

```ts
Record<string, string>
```

但实际语言包通常是嵌套结构：

```json
{
  "login": {
    "welcomeBack": "欢迎回来"
  }
}
```

建议改为：

```ts
type LocaleMessage = {
  [key: string]: string | LocaleMessage;
};

export type ImportLocaleFn = () => Promise<{ default: LocaleMessage }>;
export type LoadMessageFn = (lang: SystemLanguage) => Promise<LocaleMessage | undefined>;
```

---

### 8.5 业务包装层建议透传 options

当前业务包装层接收了 `options`，但没有使用：

```ts
export const setupI18n = async (app: App, options: LocaleSetupOptions = {}) => {
  await coreSetup(app, {
    defaultLocale: SystemLanguage.ZhCn,
    loadMessages,
  });
};
```

如果希望外部可以覆盖默认语言，建议改成：

```ts
export const setupI18n = async (app: App, options: LocaleSetupOptions = {}) => {
  await coreSetup(app, {
    defaultLocale: options.defaultLocale || SystemLanguage.ZhCn,
    loadMessages: options.loadMessages || loadMessages,
  });
};
```

如果既要保留业务语言包，又要允许外部扩展，可以合并两个 `loadMessages`：

```ts
export const setupI18n = async (app: App, options: LocaleSetupOptions = {}) => {
  await coreSetup(app, {
    defaultLocale: options.defaultLocale || SystemLanguage.ZhCn,
    async loadMessages(lang) {
      const businessMessages = await loadMessages(lang);
      const extraMessages = await options.loadMessages?.(lang);

      return {
        ...businessMessages,
        ...extraMessages,
      };
    },
  });
};
```

---

## 9. 总结

[packages/platform/core/locales/src/i18n.ts](packages/platform/core/locales/src/i18n.ts) 是平台国际化能力的核心入口。

它的设计特点是：

- 使用 `createI18n` 创建全局国际化实例。
- 开启 `globalInjection`，支持模板直接使用 `$t`。
- 使用 `legacy: false`，适配 Vue 3 Composition API。
- 使用 `import.meta.glob` 自动扫描语言包。
- 使用 `loadLocalesMapFromDir` 按语言目录合并 JSON。
- 支持业务应用通过 `loadMessages` 扩展语言包。
- 初始化时自动加载默认语言，并设置 `<html lang>`。

后续如果要支持运行时语言切换，建议补充：

1. 导出 `loadLocaleMessages`。
2. 增加已加载语言缓存。
3. 对 `mergeLocaleMessage` 的空值做保护。
4. 优化语言包类型，支持嵌套对象。
