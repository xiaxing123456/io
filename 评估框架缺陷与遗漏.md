我做的是静态排查，没有改文件、也没有执行构建/测试。整体判断：这套框架目前还偏“半成品脚手架”，主要问题集中在 构建链路断裂、运行时配置不稳、主框架壳未接真实业务、鉴权/错误处理缺失、组件库 public API 与实现不一致。

结论优先级
P0：会直接导致启动/构建/生产运行失败
根构建脚本指向不存在的包名

根 package.json:11 使用 @io-platform/common，但实际包名是 packages/platform/core/common/package.json:2 的 @io-platform/core-common。
影响：build:common、init:dev 链路会断。
admin-vue 的 build 脚本引用了不存在的子脚本

packages/platform/admin-vue/package.json:6-9 里调用 type-check、build-only，但该包没有定义这两个脚本。
影响：主应用构建不可用。
Vite 配置在 ESM 包里使用 __dirname

packages/platform/admin-vue/package.json:5 是 type: module。
packages/platform/admin-vue/vite.config.ts:13-18 直接使用 __dirname。
影响：Vite 配置加载阶段可能直接报 __dirname is not defined。
生产环境会访问未初始化的 window.DMS_PIMC_PROXY

packages/platform/admin-vue/src/utils/env-var.ts:1-19 只在 DEV 下设置 window.DMS_PIMC_PROXY，但后面无条件读 window.DMS_PIMC_PROXY.baseUrl。
影响：生产包初始化可能白屏。
store 加密密钥变量名不一致

代码读的是 packages/platform/admin-vue/src/stores/setup.ts:20-23 的 VITE_APP_STORE_SECURE_KEY。
环境文件定义的是 packages/platform/admin-vue/.env:7-11 的 VITE_STORE_SECURE_KEY。
影响：持久化加密配置实际可能是空值，登录态/用户信息持久化不可靠。
core-ui 构建脚本引用了不存在的复制声明文件脚本

packages/platform/core/ui/package.json:18-22 调用 node ./scripts/copy-type-declarations.mjs，但当前包下没有 scripts 目录。
影响：组件库 build 会在类型阶段失败。
文档构建/预览链路不一致

根脚本预览 docs/dist：package.json:13-14。
实际文档构建输出 docs/docs-portal：script/build-all-docs.js:86-96。
另外 docs/doc-blog/package.json:7-9 调用了缺失的 script/traverse-derectory.mjs。
影响：文档构建/预览流程不稳定。
dev-tools 的 ESM 入口路径拼错

packages/platform/dev/tools/package.json:5-7 写的是 dist/ems/index.js。
实际 tsconfig 输出是 packages/platform/dev/tools/tsconfig.json:3-6 的 dist/esm。
影响：依赖 module 字段的消费者会加载失败。
P1：主框架能力缺口
主布局没有渲染业务页面

packages/platform/admin-vue/src/views/layout/main/main.vue:15-20 主内容区是硬编码 222，没有嵌套 router-view。
影响：即使路由配置好了，也没有承载业务页面的位置。
侧边栏菜单还是 demo

packages/platform/admin-vue/src/views/layout/sidebar/menu/menu.vue:1-34 菜单硬编码，点击只 console.log，没有 router.push、权限过滤、动态菜单。
影响：框架壳看起来有菜单，但不能真正导航。
路由守卫只判断是否登录，没有角色/权限控制

packages/platform/admin-vue/src/router/tools/guard.ts:28-64 只处理忽略登录、登录页、无 token 跳登录。
packages/platform/admin-vue/src/router/index.type.ts:3-13 route meta 也只有忽略登录/历史相关字段。
影响：登录用户只要知道地址就可能访问页面；菜单、按钮、路由权限无法统一。
Element Plus 国际化没有和业务 i18n 联动

packages/platform/admin-vue/src/App.vue:1-4 使用了 el-config-provider，但没有传 Element Plus locale。
影响：业务文案切换语言后，Element Plus 内置组件文案可能不跟随切换。
应用直接引用组件库内部 src 路径

packages/platform/admin-vue/src/composables/use-api/index.ts:1-5 从 @io-platform/core-ui/src/... 引入。
影响：绕过组件库 public API，后续 core-ui 构建产物、exports、目录调整都会破坏 admin-vue。
P1：登录、鉴权、安全、错误处理问题
token 过期判断没有被路由守卫使用

packages/platform/admin-vue/src/stores/modules/core-access/index.ts:32-40 的 getAccessToken() 会判断过期并清理。
但守卫直接读 packages/platform/admin-vue/src/router/tools/guard.ts:36-48 的 coreAccess.accessToken。
影响：过期 token 仍可能被当作已登录状态。
退出登录只调接口，不清本地状态

packages/platform/admin-vue/src/views/layout/header/components/user-avatar-button/user-avatar-button.vue:80-94 只调用 logout。
token 清理方法存在于 packages/platform/admin-vue/src/stores/modules/core-access/index.ts:49-52，但退出流程没用。
用户信息还会持久化：packages/platform/admin-vue/src/stores/modules/user-access/index.ts:77-80。
影响：点退出后本地仍可能保持登录态和用户信息。
HTTP 全局错误处理为空

packages/platform/admin-vue/src/utils/request/common/hooks.ts:19-31 只处理业务 success，onHttpError 是空实现。
影响：401/403、网络错误、超时、服务端异常都没有统一兜底；token 失效不会自动清理和跳登录。
请求取消管理器会打印完整请求配置

packages/platform/core/http/src/core/RequestCancellationManager.ts:38-55 有 console.log('attachCancellation', config)。
packages/platform/core/http/src/core/RequestCancellationManager.ts:98-117 也有调试输出。
影响：headers、token、请求体、用户数据可能暴露在控制台。
请求取消会覆盖调用方传入的 AbortSignal

packages/platform/core/http/src/core/RequestCancellationManager.ts:38-41 直接 config.signal = controller.signal。
影响：业务层已有取消机制会失效。
登录提交 catch 了错误但完全吞掉

packages/platform/admin-vue/src/views/login/login.vue:176-188 的 catch (error) {} 为空。
影响：表单校验、加密失败、登录失败都可能被静默吞掉，调用方也无法感知失败。
token 和用户信息持久化在前端存储里

token 持久化：packages/platform/admin-vue/src/stores/modules/core-access/index.ts:60-64。
用户信息持久化：packages/platform/admin-vue/src/stores/modules/user-access/index.ts:77-80。
生产使用前端可见密钥加密：packages/platform/admin-vue/src/stores/setup.ts:20-40。
影响：这只能算混淆，不是强安全边界；一旦 XSS，token 仍可被拿到。
P1/P2：i18n 与异步可靠性
i18n 没有 fallback

packages/platform/core/locales/src/i18n.ts:25-30 创建 i18n 时没有配置 fallbackLocale。
影响：语言包缺 key 或缺文件时，页面容易直接显示 key 或出现半翻译状态。
业务语言包可能返回 undefined，然后被 merge

packages/platform/admin-vue/src/i18n/setup.ts:14-17 找不到语言包时返回 undefined。
packages/platform/core/locales/src/i18n.ts:97-100 直接 mergeLocaleMessage(lang, mergeMessage)。
影响：缺失语言包时缺少安全兜底。
语言切换存在并发竞态

packages/platform/admin-vue/src/views/layout/header/components/user-avatar-button/user-avatar-button.vue:67-70 快速切换语言时直接 await。
packages/platform/core/locales/src/i18n.ts:85-102 没有序列号/取消机制。
影响：后发请求可能先完成，最终界面语言与用户最后选择不一致。
P1/P2：core-ui 组件库问题
文档推荐的子路径导入和 package 发布配置不匹配

文档使用 @io-platform/core-ui/components：docs/doc-ui/index.md:52-53。
但 packages/platform/core/ui/package.json:5-11 只声明了 main/module/types/files，没有 exports 子路径。
影响：消费者按文档导入可能 module not found。
PltTree 传入不存在 id 会直接崩

packages/platform/core/ui/src/components/plt-tree/plt-tree.vue:453-493 中 setCheckedRow、setTreeExpand 对单个 id 或数组 id 没有统一空值保护。
影响：数据刷新后传入旧 id，会触发 undefined.id。
PltTree 拖拽目标索引硬编码

packages/platform/core/ui/src/components/plt-tree/plt-tree.vue:520-524 把 newDraggableIndex 写死为 5。
影响：拖拽排序逻辑不可信，真实场景会错位。
PltMenu 不具备 SSR/预渲染安全性

packages/platform/core/ui/src/components/plt-menu/src/composables/use-tree/index.ts:251-254 直接使用 window.location.origin。
packages/platform/core/ui/src/components/plt-menu/src/components/cascader-menu.tsx:31-35 setup 阶段直接读 document.documentElement。
影响：SSR、预渲染、测试环境容易报 window/document is not defined。
菜单 key 为 0 时会被错误过滤掉

packages/platform/core/ui/src/components/plt-menu/src/composables/use-tree/index.ts:764-767 使用 .filter(Boolean)。
影响：合法 key 0 会被当成 false 删除。
Tree 高亮工具函数没有真正按 className 匹配

packages/platform/core/ui/src/components/plt-tree/helper.ts:252-278 replaceVNodeByClass 接收 className，但逻辑并未用它判断，只要 children 是字符串就替换。
影响：高亮搜索可能改写不该改写的文本节点。
高亮渲染只取 slot 的第一个 vnode

packages/platform/core/ui/src/components/plt-tree/helper.ts:293-303 只解构 const [dom] = slots.default(params)。
影响：slot 返回 icon + label + badge 这类多个节点时，高亮场景可能丢内容。
缺少考虑点
CI 质量门禁缺失

当前只看到文档部署 workflow：.github/workflows/deploy-docs.yml:57-60。
缺少 workspace 级别的 build、type-check、lint、test、组件库打包验证。
包边界和 public API 没有稳定规范

admin-vue 直接引用 core-ui 的 src。
core-ui 文档写了子路径导入，但 package 没有 exports。
建议统一：只允许从包 public entry 导入，并补 exports map。
鉴权体系还没成型

缺少 route meta 权限字段。
缺少菜单权限、按钮权限、接口 401/403 联动。
缺少 token refresh、会话过期、单点登录回跳等完整策略。
错误处理体系不完整

缺少 HTTP 错误分级。
缺少路由错误兜底。
缺少全局未捕获异常处理。
缺少日志上报/监控埋点策略。
运行时配置策略不清晰

DEV 代理、生产 baseUrl、微服务名、window 全局配置之间没有清晰边界。
建议统一为“构建时 env + 可选运行时 config.js”两层模式。
安全边界需要重新定义

前端加密存储不能当作安全能力。
需要考虑 CSP、XSS 防护、敏感日志清理、token 生命周期、HttpOnly Cookie 或 refresh token 策略。
i18n 需要完整兜底

fallbackLocale。
缺 key 检测。
Element Plus locale 联动。
语言切换竞态处理。
语言包缺失时降级。
组件库缺少测试与边界用例

Tree/Menu 这类复杂组件至少需要覆盖：空数据、动态刷新、id 为 0、拖拽、slot 多节点、SSR/no-window 环境、超大数据。
建议修复顺序
先修 P0 构建链路：package scripts、Vite __dirname、env 变量、core-ui build、docs build。
再补主框架闭环：main router-view、真实菜单导航、登录退出、401/403、权限 meta。
规范包 API：core-ui exports、禁止 admin-vue 引用 core-ui/src、文档示例跟实际发布一致。
补质量门禁：最小 CI 先跑 install、admin-vue build、core-ui build、core-common build、docs build。
最后补体验与长期能力：i18n fallback、错误上报、安全策略、组件边界测试。